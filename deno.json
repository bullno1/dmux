{
  "tasks": {
    "start": "deno run --allow-all main.ts",
    "check": "deno check main.ts && deno lint",
    "compile": "deno compile --allow-all --output bin/dmux main.ts"
  },
  "unstable": ["net", "kv"]
}
